---
phase: directory-contract
task: 4
total_tasks: 7
status: pending
last_updated: 2026-03-05T00:05:00
---

# CONTINUE HERE - Directory Contract (Aria @architect)

## CURRENT STATE
Tasks 1-3 COMPLETAS e commitadas. Tasks 4-7 pendentes.
Context exhausted before Task 4 could start.

## COMPLETED
- [x] Task 1: `core/paths.py` criado (PATHS + ROUTING dict + PROHIBITED)
- [x] Task 2: `.claude/rules/directory-contract.md` criado (contrato human-readable)
- [x] Task 3: `.claude/hooks/directory_contract_guard.py` criado (PreToolUse WARN)
- [x] `artifacts/audit/.gitkeep` criado
- Commits: `2fc1754b`, `2c837fbb`

## REMAINING (execute in order)

### Task 4: Migrar docs/
```bash
# Tracked files in docs/ that need git mv:
git mv docs/API-KEYS-GUIDE.md reference/
git mv docs/LAYERS.md reference/
git mv docs/INTEGRATION-POINTS.md reference/
git mv docs/VERIFICATION-LEVELS.md reference/
git mv docs/RESTORE-AND-INDEX.md reference/
git mv docs/TAG-RESOLVER-IMPLEMENTATION.md reference/
git mv docs/DEVOPS-AUDIT-LAYER-SYSTEM.md reference/
git mv docs/PLAN-MODE-PROTOCOL.md reference/

# Check if docs/prompts/ is tracked:
git ls-files docs/prompts/
# If tracked: git mv docs/prompts/meta_agente_mapeamento_processos.md .claude/commands/

# Duplicatas → .claude/trash/ (check which are tracked vs untracked first)
# git ls-files docs/ to see what's left tracked
# For tracked: git rm then move to .claude/trash/
# For untracked: just mv to .claude/trash/
mkdir -p .claude/trash/docs-migration-20260305
```

### Task 5: Update 3 Python scripts
- `core/intelligence/audit_layers.py`: change `docs/audit/` → import from core.paths ROUTING["audit_report"]
- `core/intelligence/validate_layers.py`: same
- `core/intelligence/verify_classifications.py`: same

### Task 6: Update configs
- `package.json`: replace ~21 `"docs/..."` entries with `"reference/..."`
- `.claude/CLAUDE.md`: update architecture tree (remove docs/, confirm reference/)
- `.gitignore`: add `artifacts/audit/` and `docs/audit/`

### Task 7: Register hook + QA
- Add directory_contract_guard.py to settings.json PreToolUse
- Run verification: `git ls-files docs/ | wc -l` should be 0
- Test hook: write to docs/ should warn

## RESTRICTIONS
1. NÃO mexer em `research/`
2. NÃO deletar — mover para `.claude/trash/`
3. Não quebrar paths
4. Enforcement total

## COMMAND TO RESUME
```
Continuar Directory Contract. Tasks 1-3 done, 4-7 pendentes.
Task 4: migrar docs/ (git mv úteis para reference/, duplicatas para .claude/trash/)
Plano completo: ~/.claude/plans/refactored-spinning-rivest.md
```
